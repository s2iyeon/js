// 유니언 타입(union type)

(()=>{
  // 문자열을 받아서 콘솔에 출력하는 함수
  function logString(msg: string) {
    console.log(msg);
  }
  // 숫자를 받아서 콘솔에 출력하는 함수
  function logNumber(msg: number) {
    console.log(msg);
  }



})();